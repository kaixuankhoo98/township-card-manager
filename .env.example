# Frontend (Vite) — safe to expose to the browser, read from the Supabase project dashboard
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Used only by scripts/syncCatalog.ts (never sent to the browser). Keep this
# out of version control — copy this file to .env.local and fill it in there.
SUPABASE_SERVICE_ROLE_KEY=
